# nollm-wiki

Deterministic wiki compilers that organize a project into an agent-navigable
knowledge base without an LLM at generation time.

- `wiki/` — the Rust implementation (current). See `wiki/README.md`.
- `wiki-compiler/` — the original Python prototype (reference).

Design and plan docs live under `docs/superpowers/`.
